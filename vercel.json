{
    "builds": [
      {
        "src": "wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "env": {
      "TESSERACT_PATH": "/usr/bin/tesseract"
    },
    "installCommand": "apt update && apt install -y tesseract-ocr"
  }
  